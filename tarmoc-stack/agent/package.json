{
  "name": "tarmoc-agent",
  "version": "1.0.0",
  "private": true,
  "description": "TARMOC monitoring agent — reads real CPU/RAM/Disk/Network from the host, pushes to the TARMOC API",
  "main": "agent.js",
  "scripts": { "start": "node agent.js" },
  "engines": { "node": ">=18" },
  "dependencies": {}
}
